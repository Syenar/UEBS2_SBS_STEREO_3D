# Legacy notes — superseded

[`UEBS2_3D_VR_Mod_Plan.txt`](../UEBS2_3D_VR_Mod_Plan.txt) is **historical discovery only**.

Authoritative Phase 1 architecture, wiring, UI/controls, and acceptance: [`PLAN.md`](PLAN.md).

On conflict (for example “keep original as left eye” or “no doubled UI” in the `.txt`), **ignore the `.txt`** and follow `PLAN.md`:

- Center/control camera + child eye cameras
- Same HUD image duplicated into both packed halves
- Catch-alls, dual-mono while `stereoEngaged`, F8-only true mono

When agent mode is available, prepend the same supersession banner to `UEBS2_3D_VR_Mod_Plan.txt` itself.
